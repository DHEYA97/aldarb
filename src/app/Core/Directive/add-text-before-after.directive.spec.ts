import { AddTextBeforeAfterDirective } from './add-text-before-after.directive';

describe('AddTextBeforeAfterDirective', () => {
  it('should create an instance', () => {
    const directive = new AddTextBeforeAfterDirective();
    expect(directive).toBeTruthy();
  });
});
